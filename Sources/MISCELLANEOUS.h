#ifndef MISCELLANEOUS_H
#define MISCELLANEOUS_H

// A file that holds some usefull functions

// Delay method in milliseconds
void DelayMilliseconds(int);

// Delay method in microseconds
void DelayMicroseconds(int);

// Count the actual running time in milliseconds
unsigned long Millis();

// Count the actual running time in microseconds
unsigned long Micros();

// Count the actual running time in nanoseconds
unsigned long Nanos();

#endif // MISCELLANEOUS_H